import { Grid } from '@mui/material';
import './Header.scss';

const Header = () => {
  return (
    <Grid container className="header">
      <h2>Note Stream</h2>
    </Grid>
  );
}

export default Header;